{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "solotribe-next",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "export NVM_DIR=\"$HOME/.nvm\" && [ -s \"$NVM_DIR/nvm.sh\" ] && . \"$NVM_DIR/nvm.sh\" && npx next dev -p 3000"],
      "port": 3000
    }
  ]
}
